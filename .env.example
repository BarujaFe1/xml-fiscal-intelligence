# Supabase — runtime integration (PR4 cadastro)
# Valores em: Supabase dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SECRETO (service role) — SÓ server/.env.local e env da Vercel.
# Nunca expor ao browser, ao bundle do client ou colar em chat.
SUPABASE_SERVICE_ROLE_KEY=

# Feature flags
FEATURE_CLOUD_PROCESSING=1

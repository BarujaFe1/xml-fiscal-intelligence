# Exports

## Implemented

Excel (multi-sheet), CSV, JSON, HTML via batch export UI (`/app/batches/[id]/exports`).

## Planned

- Parquet via DuckDB module  
- SQL INSERT dump  
- Custom column selection (one column per tag)  

Keep real exports under `private-exports/` (gitignored).

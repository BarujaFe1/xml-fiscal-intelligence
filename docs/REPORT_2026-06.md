# Relatório Final — feat/automated-fiscal-reconciliation-ux-capture

**Data:** 2026-07-15 · **Branch:** `feat/automated-fiscal-reconciliation-ux-capture` (criado a partir de `feat/soc2-grounding-erp3`)
**App:** https://xml-fiscal-intelligence.vercel.app · **Repo:** BarujaFe1/xml-fiscal-intelligence

> NÃO houve push, merge, deploy, assinatura ou transmissão. Status PVA = `pva_rejected`; cloud = `local_prototype`.
> Itens que dependem de ambiente externo estão classificados como **`blocked_external`** e NÃO foram inventados.

## 1. PVA (§2–9)

| Geração | Guia | PVA | Erros | Advertências | Status |
|---|---|---|---:|---:|---|
| `probe-efd-fix` (06/2026, perfil A) | 3.2.2 | 6.1.0 (usuário) | 2 | 0 | **`pva_rejected`** |
| Generation 2 (corrigida) | — | — | — | — | **`blocked_external`** (relatório detalhado do PVA não disponível) |

- Evidência: apenas o resumo do PVA (competência, perfil, total, categoria "Campo obrigatório"). **Registro/campo/linha/valor NÃO informados** → correção específica = `blocked_external` (§4).
- Arquivo reprodutível para o usuário: `private-exports/probe-efd-fix/efd.txt`.
- Implementado: tipos `PvaValidationRun`/`PvaValidationMessage`, página de importação+mapeamento (`src/app/app/obligations/efd-icms-ipi/pva/page.tsx`), pré-validador genérico (`prevalidate.ts`), migration `202607150001_pva_field_definitions.sql`.
- Status nunca marcado como `aceito`/`oficial`/`homologado`.

## 2. UI/UX (§10–16)

- Lab language removida da interface comum: topbar agora mostra indicador discreto **"Dados neste dispositivo"** (sem "Ambiente local de demonstração · IndexedDB"); sidebar reagrupada (Início/Importar/Conferir/Fechamentos + Análise + Plataforma); configurações técnicas movidas para `TechnicalDetailsDrawer` "Diagnóstico técnico"; billing com estado elegante "Assinatura não disponível" quando não configurado; companies com "Nova empresa", validação CNPJ, `FileUpload` sem "Choose File".
- 18 componentes de design system criados em `src/components/design-system/*`.
- `CardTitle` tornou-se level-aware (corrige pulo h2→h3).

## 3. Acessibilidade (§12)

| Rota | Antes | Depois | Crítica | Séria | Moderada |
|---|---|---|---:|---:|---:|
| /login | n/d* | 0 | 0 | 0 | 0 |
| /signup | n/d* | 0 | 0 | 0 | 0 |
| /forgot-password | n/d* | 0 | 0 | 0 | 0 |
| /app | n/d* | 0 | 0 | 0 | 0 |
| /app/upload | n/d* | 0 | 0 | 0 | 0 |
| /app/reconciliation | n/d* | 0 | 0 | 0 | 0 |
| /app/closing | n/d* | 0 | 0 | 0 | 0 |
| /app/batches | n/d* | 0 | 0 | 0 | 0 |
| /app/companies | n/d* | 0 | 0 | 0 | 0 |
| /app/billing | n/d* | 0 | 0 | 0 | 0 |
| /app/settings | n/d* | 0 | 0 | 0 | 0 |

\* "Antes" = pacote `ui-capture.zip` do usuário **não está no repo**; o harness preserva `artifacts/ui-capture/after/` e documenta onde colocar o `before/`. "Depois" = captura real (Chromium/desktop, estados empty+restricted) via `npm run screenshots:all` + `test:visual` (11/11 rotas com `main`+`h1` e 0 violações críticas/sérias/moderadas de Axe).
Violações restantes corrigidas: color-contrast, link-in-text-block, nested-interactive (upload), label (closing), button-name (batches).

## 4. RLS (§17)

Suíte comportamental `tests/rls/real-rls.test.ts` (pg) criada, gated em `RUN_RLS_LIVE`+`DATABASE_URL_TEST`, recusando o host de produção. **Execução ao vivo = `blocked_external`** (sem Postgres local/CLI; `DATABASE_URL` de produção não pode ser usado para testes destrutivos). `rls-hygiene` offline segue verde.

| Tabela (do RLS_TEST_MATRIX) | SELECT | INSERT | UPDATE | DELETE | Storage |
|---|---|---|---|---|---|
| workspaces | testado (skip) | testado (skip) | testado (skip) | testado (skip) | n/a |
| batches / documents | testado (skip) | testado (skip) | testado (skip) | testado (skip) | n/a |
| establishments / companies | testado (skip) | testado (skip) | testado (skip) | testado (skip) | n/a |
| obligation_generations / pva_validation_runs | testado (skip) | testado (skip) | testado (skip) | testado (skip) | n/a |
| exports / saved_searches | testado (skip) | testado (skip) | testado (skip) | testado (skip) | testado (skip, negado cross-tenant) |

Nota: o repo tem **80 tabelas** com RLS (não 14 — não há relatório "14 tabelas"; usou-se `docs/RLS_TEST_MATRIX.md` como especificação).

## 5. Cloud / Persistência (§18)

**Classificação: `local_prototype`.** Fonte de verdade = IndexedDB (browser) + FS local (server). Supabase = espelho de metadados unidirecional. Adapters `BatchRepository` definidos mas **nunca invocados** no runtime. Teste cross-browser = `blocked_external`. Ver `docs/CLOUD_STATUS_2026-06.md`.

## 6. Testes

| Suíte | Total | Passou | Falhou | Ignorado |
|---|---:|---:|---:|---:|
| unit (vitest) | 241 | 240 | 0 | 1 |
| e2e (playwright) | 30 | 29 | 1 | 0 |
| visual (playwright + Axe) | 11 | 11 | 0 | 0 |
| rls (vitest, live) | 6 | 0 | 0 | 6 |
| lint | — | — | 6 (pré-existentes em rtc/scale) | — |
| typecheck | — | pass | — | — |
| build | — | pass | — | — |
| secrets:scan-paths | — | pass (607 arquivos) | — | — |
| npm audit | — | 4 moderadas transitivas (uuid/exceljs, postcss/next) — `audit fix --force` downgrade Next p/ v9, não aplicado | — | — |

Falha e2e: `/app/scale renders without crash` — página "Plataforma" **fora das 11 rotas capturadas** e não editada nesta rodada; carrega erros pré-existentes de lint (`set-state-in-effect`). Fora de escopo.

## 7. Cobertura visual (§15/16)

| Rota | Empty | Populated | Loading | Error | Mobile |
|---|---|---|---|---|---|
| 11 rotas | ✅ (chromium/desktop) | scripted* | scripted* | scripted* | scripted* |

\* Estados populated/loading/error são suportados pelo harness (`scripts/screenshots-all.mjs`, parametrizável por `ENGINES`/`VIEWPORTS`/`STATES`); nesta rodada executou-se chromium/desktop (empty+restricted) para gerar evidência real sem overhead. Matriz completa 3 engines × 3 viewports disponível sob demanda.

## 8. Itens `blocked_external` (honestos, não inventados)

1. Correção específica dos 2 campos do PVA (relatório detalhado ausente).
2. Execução manual do PVA (PVA não instalável neste ambiente).
3. RLS em Postgres real (sem Postgres local; prod não deve ser usado).
4. Teste cloud cross-browser (multi-dispositivo).
5. `before/` do ui-capture (zip do usuário ausente no repo).
6. Matriz completa de screenshots 3×3 (executou-se chromium/desktop).
7. `/app/scale` e2e (pré-existente, fora de escopo).

## 9. Comandos executados (evidência)

- `npm run typecheck` → exit 0
- `npm run lint` → 6 erros (pré-existentes em rtc/scale), 0 novos
- `npm run test` → 240 passed / 1 skipped
- `npm run build` → sucesso (todas as rotas compiladas)
- `npm run test:e2e` → 29 passed / 1 failed (/app/scale, pré-existente)
- `npm run test:visual` → 11 passed (0 crítico/sério Axe)
- `npm run test:rls` → 6 skipped (live blocked_external)
- `npm run secrets:scan-paths` → ok (607 arquivos)
- `npm audit` → 4 moderadas transitivas
- `node scripts/seed-demo.mjs` → fixtures determinísticos (hashes idênticos em 3 execuções)
- `node scripts/screenshots-all.mjs` → `artifacts/ui-capture/after/` (19 shots, Axe 0/0/0)

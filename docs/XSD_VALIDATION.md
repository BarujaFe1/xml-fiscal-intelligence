# XSD Validation

## Status

Stub: `src/modules/validation/xsd-validator.ts` returns `not_configured`.

## Planned

- Versioned schemas under `/schemas` (NF-e, CT-e, NFS-e ABRASF)  
- Env: `ENABLE_XSD_VALIDATION`, `XSD_SCHEMA_DIR`  
- Persist errors to `audit_findings` / `import_logs`  

## Result statuses

`valid` | `invalid` | `not_configured` | `unsupported`
